apiVersion: v1
kind: Service
metadata:
  name: devpod-ssh
  namespace: default
spec:
  type: NodePort
  selector:
    app: devpod  # 必须与 Deployment 中的 label 匹配
  ports:
    - protocol: TCP
      port: 22       # Service 内部端口
      targetPort: 22 # 容器端口
      nodePort: 30022 # 宿主机/节点端口 (范围 30000-32767)
      